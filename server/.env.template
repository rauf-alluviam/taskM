# Production Environment Variables Template
# Copy this file to .env and fill in your actual values

# === REQUIRED VARIABLES ===

# JWT Secret - MUST be a strong, random string (minimum 32 characters)
# Generate with: openssl rand -base64 48
JWT_SECRET=your-super-secure-jwt-secret-here-minimum-32-characters

# MongoDB Connection String
MONGODB_URI=mongodb://localhost:27017/taskflow

# Email Configuration (for sending invitations and notifications)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# === OPTIONAL VARIABLES ===

# Client URL (frontend application URL)
CLIENT_URL=http://localhost:5173

# Server Port
PORT=5000

# Environment
NODE_ENV=production

# Email Host Configuration (if not using Gmail)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587

# === PRODUCTION DEPLOYMENT CHECKLIST ===

# 1. Set JWT_SECRET to a strong, unique value
# 2. Update MONGODB_URI to your production database
# 3. Configure email settings for your email provider
# 4. Set CLIENT_URL to your production frontend URL
# 5. Set NODE_ENV=production
# 6. Ensure all sensitive values are kept secure
# 7. Run: node check-env.js to validate your environment

# === SECURITY NOTES ===

# - Never commit this file with real values to version control
# - Use environment variables or secure vault in production
# - Rotate JWT_SECRET periodically
# - Use app-specific passwords for email authentication
# - Keep database credentials secure
